# CLAUDE.md

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고할 지침을 제공합니다.

## 프로젝트 개요
- **서비스명:** 롤링페이퍼 (rolling-paper)
- **목표:** 20~30명이 함께 작성한 메시지를 **풀숲의 꽃처럼 시각적으로** 담아, 감사한 분께 선물하는 **모바일 웹 롤링페이퍼 서비스**.
- **핵심 사용자 경험:** 여러 명이 메시지를 남기면 한 화면에 자연 풍경(풀숲/꽃) 메타포로 모여 보이고, 받는 사람에게 한 편의 선물처럼 전달된다.
- **타깃 디바이스:** 모바일 웹 우선(반응형), 작성자·수신자 모두 모바일에서 자연스럽게 동작해야 함.

## 작업 방침
- **새 기능을 추가할 때 기존 파일을 크게 바꾸기 전에 반드시 설명하고 확인받는다.**
  - 변경 범위가 큰 리팩터링·구조 변경·공통 모듈 수정 등은 먼저 무엇을·왜·어떻게 바꿀지 요약해 사용자 확인을 받은 뒤 착수한다.
  - 단순 오타 수정, 신규 파일 추가, 사용자가 명시적으로 지시한 변경은 곧바로 진행해도 된다.

## 언어 규칙
- 사용자와의 모든 대화는 **한국어**로 한다.
- 코드 주석은 **한국어**로 작성한다.
- 새로 생성하거나 편집하는 마크다운(`.md`) 파일의 본문은 **한국어**로 작성한다.
- 단, 코드 식별자(변수/함수/클래스명), API 응답, 외부 도구 명령어, 라이브러리 고유 명사 등은 영어 원문을 유지한다.
- 사용자가 특정 응답에서 명시적으로 다른 언어를 요청하면 그 요청을 우선한다.
