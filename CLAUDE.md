# CLAUDE.md

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고할 지침입니다.

## 프로젝트 목적
- **가정의 달 롤링페이퍼 모바일 웹 서비스**
- 20~30명이 함께 작성한 메시지를 풀숲의 꽃처럼 시각적으로 담아, 감사한 분께 선물한다.
- 작성자·수신자 모두 **모바일 웹 우선**(반응형) 환경에서 자연스럽게 동작해야 한다.

## 기술 스택
- **빌드/프레임워크:** Vite + React 18 + TypeScript
- **스타일:** Tailwind CSS (PostCSS + Autoprefixer)
- **애니메이션:** framer-motion
- **상태 관리:** zustand
- **라우팅:** react-router-dom
- **백엔드:** Supabase (`@supabase/supabase-js`)
- **이미지 캡처:** html2canvas, dom-to-image

## 절대 규칙 (변경 금지)
- **이미지 업로드 기능은 절대 추가하지 않는다.** (스토리지 유지비용 이슈)
- **풀숲 테마 1종만 구현한다.** (나무 테마는 v1.1로 미루기)
- **모든 메시지 카드는 동일한 깔끔한 디자인 1종**만 사용한다. (카드 스타일 선택 기능 없음)
- **별도 스티커 셋을 만들지 않는다.** 이모지는 시스템 키보드로 본문 안에 입력한다.
- **부적절 콘텐츠 신고 기능은 만들지 않는다.**
- **받는 분 하트 리액션 기능은 만들지 않는다.**
- **한 줄/편지 모드 토글을 만들지 않는다.** 작성창은 단일 형태로 유지한다.

## 코드 규칙
- 파일은 `src/features/*` 폴더로 **기능 단위 모듈화**한다.
- 한 파일이 **300줄을 넘으면 분할을 검토**한다.
- 공용 UI 컴포넌트는 `src/components`, 유틸/클라이언트는 `src/lib`, 라우트 페이지는 `src/pages`, 타입은 `src/types`에 둔다.
- 작성하는 코드 주석은 한국어로 한다.

## 작업 방침
- **새 기능을 추가할 때 기존 파일을 크게 바꾸기 전에 반드시 설명하고 확인받는다.**
  - 변경 범위가 큰 리팩터링·구조 변경·공통 모듈 수정 등은 먼저 무엇을·왜·어떻게 바꿀지 요약해 사용자 확인을 받은 뒤 착수한다.
  - 단순 오타 수정, 신규 파일 추가, 사용자가 명시적으로 지시한 변경은 곧바로 진행해도 된다.

## 언어 규칙
- 사용자와의 모든 대화는 **한국어**로 한다.
- 코드 주석, 커밋 메시지, 마크다운(`.md`) 본문은 모두 **한국어**로 작성한다.
- 단, 코드 식별자(변수/함수/클래스명), API 응답, 외부 도구 명령어, 라이브러리 고유 명사 등은 영어 원문을 유지한다.
- 사용자가 특정 응답에서 명시적으로 다른 언어를 요청하면 그 요청을 우선한다.

## 폴더 구조
```
src/
  features/         기능별 폴더 (room-create, message-write, host-dashboard, gift-wrap, viewer, image-export)
  components/       공용 UI 컴포넌트
  lib/              supabase 클라이언트, 유틸 함수
  pages/            라우트 페이지
  types/            TypeScript 타입
```
